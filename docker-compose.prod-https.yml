services:
  nginx:
    volumes:
      - ./:/www/registrasi-online:ro
      - ./docker/nginx/default-ssl.conf:/etc/nginx/conf.d/default.conf:ro
      - certbot-www:/var/www/certbot
      - certbot-etc:/etc/letsencrypt
